import styled from "styled-components";

const StyledStoreInfo = styled.div`
  width: 35%;
  height: auto;
  border-right: 1px solid rgb(238, 238, 238);
`;

const StoreInfo = () => {
  return <StyledStoreInfo></StyledStoreInfo>;
};

export default StoreInfo;
