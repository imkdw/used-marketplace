import styled from "styled-components";

const StyledRecoverPassword = styled.div``;

const RecoverPassword = () => {
  return <StyledRecoverPassword>Recover Password Page</StyledRecoverPassword>;
};

export default RecoverPassword;
